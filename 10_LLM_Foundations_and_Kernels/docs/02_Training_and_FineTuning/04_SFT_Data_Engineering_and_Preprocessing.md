# 大模型微调数据工程：格式演进、清洗实战、Loss Masking 与分布控制

> **归属模块**：`LLM_FineTuning`  
> **更新策略**：增量追加（严禁截断历史）  
> **面向对象**：人工智能专业理论与工程实践

---

## 目录 (Table of Contents)
- [1. 学习记录流水线 (Changelog)](#1-学习记录流水线-changelog)
- [2. [2026-09-08] SFT 数据工程全流程与认知纠偏](#2-2026-09-08-sft-数据工程全流程与认知纠偏)
  - [2.1 格式认知纠偏：破除“Alpaca 最通用”的过时偏见](#21-格式认知纠偏破除alpaca-最通用的过时偏见)
  - [2.2 去重算法演进：从 MD5 精确匹配到 MinHash LSH 与 SemDeDup](#22-去重算法演进从-md5-精确匹配到-minhash-lsh-与-semdedup)
  - [2.3 质量过滤的工业级标准：启发式规则 + LLM-as-a-Judge](#23-质量过滤的工业级标准启发式规则--llm-as-a-judge)
  - [2.4 格式统一的生死命门：Chat Template 对齐与 Loss Masking 机制](#24-格式统一的生死命门chat-template-对齐与-loss-masking-机制)
  - [2.5 数据平衡陷阱：为什么传统过采样/欠采样在大模型上是灾难？](#25-数据平衡陷阱为什么传统过采样欠采样在大模型上是灾难)
  - [2.6 数据泄露与污染检测 (Data Contamination)](#26-数据泄露与污染检测-data-contamination)
- [3. 专业实战测评题库 (含采分点与解析)](#3-专业实战测评题库-含采分点与解析)
- [4. 极简复习闪卡 (CheatSheet)](#4-极简复习闪卡-cheatsheet)

---

## 1. 学习记录流水线 (Changelog)
- **2026-09-08**：创建 SFT 数据工程专题笔记。纠偏“Alpaca 最通用”及“传统分类采样法”等陈旧认知；深度剖析 ShareGPT/Messages 多轮对话标准格式；详解 MinHash 语义去重、工业级启发式规则过滤；重点推导 SFT 生死攸关的 **Loss Masking（标签掩码机制）**；解构样本过采样的“背诵崩溃”陷阱及 Evol-Instruct 现代合成替代方案。

---

## 2. [2026-09-08] SFT 数据工程全流程与认知纠偏

### 2.1 格式认知纠偏：破除“Alpaca 最通用”的过时偏见

> [!CAUTION]
> **认知漏洞指正**：
> “数据格式 Alpaca 最通用”是 2023 年初期的历史认知。在现代大模型体系中，Alpaca 格式早已不是通用标准，它存在极其严重的功能缺陷！

#### 1. Alpaca 格式的局限
Alpaca 格式（`instruction`, `input`, `output`）仅适用于单轮（Single-turn）简单问答：
```json
{
  "instruction": "将以下句子翻译为英文",
  "input": "今天天气真好",
  "output": "The weather is really nice today."
}
```
- **致命缺陷**：天然不支持多轮对话（Multi-turn）、无法规范定义系统角色（System Prompt）、无法承载工具调用（Tool Calls / Function Calling）、更无法支持思维链推理标记（如 DeepSeek `<think>...</think>`）。

#### 2. 现代工业界真正的事实标准：ShareGPT / OpenAI Messages 格式
当前各大开源大模型（LLaMA-3, Qwen-2.5, DeepSeek, Mistral）官方指令微调全部采用 **多角色消息列表（Messages / ShareGPT）** 格式：
```json
{
  "messages": [
    {"role": "system", "content": "你是一名资深人工智能教授。"},
    {"role": "user", "content": "什么是 KV Cache？"},
    {"role": "assistant", "content": "KV Cache 是大模型自回归推理中以空间换时间的缓存技术..."},
    {"role": "user", "content": "它会带来显存问题吗？"},
    {"role": "assistant", "content": "会，上下文极长时显存开销会呈线性爆发..."}
  ]
}
```
**优势**：通过标准的角色属性（`system`, `user`, `assistant`, `tool`）无缝适配任意轮次的多轮交互与代理（Agent）工作流。

---

### 2.2 去重算法演进：从 MD5 精确匹配到 MinHash LSH 与 SemDeDup

数据重复会导致大模型在特定的 Token 序列上反复梯度强化，引发严重过拟合，甚至产生**退化性复读机行为（Repetition Degeneration）**。工业界去重分为三个层级：

1. **精确去重（Exact Deduplication）**：
   - 对文本计算 SHA-256 或 MD5 哈希值，建立哈希集合进行 $O(1)$ 过滤。
   - *局限*：哪怕改动一个空格或标点符号，哈希值就完全不同，无法识别抄袭改写数据。
2. **局部敏感哈希近似去重（Fuzzy Deduplication - MinHash LSH）**：
   - 将文本分词切片为 N-gram（如 5-gram），计算 Jaccard 相似度。
   - 利用 MinHash 将高维文本签名压缩为短向量，通过 LSH（Locality-Sensitive Hashing）桶索引高效找出重合度大于阈值（如 0.8）的相似数据。
3. **语义嵌入去重（Semantic Deduplication - SemDeDup, Abbas et al., 2023）**：
   - 使用开源文本 Embedding 模型将文本映射到高维稠密空间；
   - 在特征空间进行 K-Means 聚类，在簇内计算余弦相似度（Cosine Similarity），删除语义等价但字面表达不同的冗余指令（如“请帮我总结这篇文章”与“概括一下这篇文本的主旨”）。

---

### 2.3 质量过滤的工业级标准：启发式规则 + LLM-as-a-Judge

单纯删除“太短、太长、有乱码”只是最粗糙的准入规则。工业级微调的数据清洗遵循严格的漏斗模型：

```
原始海量语料
   │
   ▼ (第一层：粗粒度启发式过滤 Heuristic Filtering)
   ├── 长度阈值：剔除 Prompt < 5 字符 或 Response < 10 字符（剔除空话套话）
   ├── 压缩比测试：使用 gzip 压缩文本，剔除压缩比异常的数据（识别机械乱码、代码反编译垃圾）
   ├── 特殊符号与非自然语言比率：代码片段/数学公式占比检测，剔除 HTML 爬虫残留标签
   │
   ▼ (第二层：模式化套话与敏感词清洗 Pattern Scrubbing)
   ├── 剔除无意义免责套话（如“作为一个人工智能模型，我无法...”）
   ├── 剔除拒答样本（除非专门做对齐安全训练）
   │
   ▼ (第三层：高阶模型打分 LLM-as-a-Judge / Reward Model)
   └── 使用大模型（如 GPT-4o）对数据的【指令复杂度】、【回答准确性】、【逻辑完整性】进行 1~5 分打分，仅保留高分样本
```

---

### 2.4 格式统一的生死命门：Chat Template 对齐与 Loss Masking 机制

> [!CAUTION]
> **严重误区指正**：
> 统一中文引号、英文引号或句号，对大语言模型而言是细枝末节！LLM 在预训练时见识过海量混杂文本，对标点具有天然容忍度。
> **真正决定 SFT 生死存亡的格式统一，是对话模板（Chat Template）与标签掩码（Loss Masking）！**

#### 1. 对话模板（Chat Template / Tokenizer Config）的绝对对齐
每个预训练基模都有自己专属的特殊控制标记（Special Tokens）。微调时必须使用该基模官方规定的模板，例如 **ChatML** 规范：
```text
<|im_start|>system
你是一个数学助教。<|im_end|>
<|im_start|>user
1+1等于几？<|im_end|>
<|im_start|>assistant
1+1等于2。<|im_end|>
```
*如果数据预处理漏写了 `<|im_end|>`，或者把 `<|im_start|>` 拼错，模型在推理时将永远不知道何时停止生成，陷入无休止胡言乱语的严重瘫痪！*

#### 2. 标签掩码（Loss Masking / Instruction Masking）机制
大模型是自回归训练的，但 **SFT 阶段模型绝不能计算 Prompt（人类输入）的损失！**

```
完整输入序列:  [<|im_start|>, user, \n, 帮, 我, 算, 1, +, 1, <|im_end|>, \n, <|im_start|>, assistant, \n, 等, 于, 2, <|im_end|>]
对应 Target:   [   -100    ,  -100, -100, -100, ... , -100  , -100, -100,    -100     ,     -100    , -100, 等, 于, 2, <|im_end|>]
                               ▲                                                                           ▲
                       (Prompt 部分全部置为 -100)                                                  (仅在 Response 计算交叉熵)
```

- **物理机理**：PyTorch 交叉熵损失函数默认 `ignore_index=-100`。
- **如果不做 Loss Masking 的致命后果**：
  如果对整个序列全部计算 Loss，模型将耗费宝贵的参数容量去学习“人类会问什么样的问题”（试图背诵 User Prompt），导致其回答人类意图的泛化能力急剧下降，甚至学会在回答中自问自答。

---

### 2.5 数据平衡陷阱：为什么传统过采样/欠采样在大模型上是灾难？

> [!CAUTION]
> **分类算法直觉的严重套用错误**：
> 在传统机器学习分类（如垃圾邮件二分类）中，样本少就“过采样（重复复制）”，样本多就“欠采样（随机删除）”。
> **如果把这个套路照搬到 LLM SFT 中，会直接摧毁模型！**

#### 1. 为什么不能简单过采样（复制少数类样本）？
- 大语言模型具有极其强大的记忆参数容量。如果将少数类的 100 条样本重复复制 20 次凑成 2000 条：
  - 模型会在极短的 Epoch 内把这 100 个固定回答**倒背如流**；
  - 诱发**表征塌缩（Representation Collapse）**：面对该类别的轻微提问变形，模型只能僵化机械地吐出完全相同的固定文本，彻底丧失生成多样性。
- **正确工业方案：基于 Evol-Instruct 的多样性数据合成**：
  不重复旧数据，而是利用强模型（如 Claude-3.5-Sonnet / GPT-4o）对少数类的种子指令进行演化（深度增加约束、广度改变实体），合成出全新的、高质量的变体样本。

#### 2. 为什么不能简单欠采样（粗暴丢弃多数类样本）？
- 根据 Meta 的 **LIMA 法则（Less Is More for Alignment）**：**数据质量与覆盖的多样性，远比数量平衡更重要。**
- 粗暴随机丢弃多数类样本，会直接丢弃掉宝贵的复杂逻辑链推理样本。正确的做法是按难度、信息熵和响应质量对多数类进行分层过滤，只留精髓。

---

### 2.6 数据泄露与污染检测 (Data Contamination)

大模型数据泄露不仅包括验证集泄露，还包括**预训练语料与评测基准（Benchmarks）的重叠污染**：
1. **N-gram 碰撞检测（N-gram Overlap）**：
   - 提取测试集（如 MMLU, GSM8K, HumanEval）的 8-gram 或 13-gram 词元切片；
   - 扫描训练集，如果连续出现相同的 13-gram，判定为高危数据泄露，必须自动清洗剔除。
2. **模板逆向污染（Template Bleeding）**：
   - 严禁将与评测集完全相同 Few-Shot 提示词模板注入训练集，否则会导致虚假高分（模型仅学会了拟合基准测试的偏置，在真实场景极度脆弱）。

---

## 3. 专业实战测评题库 (含采分点与解析)

### 题 1 【训练机制推导题】
**题目**：在进行 LLaMA-3 指令微调的数据预处理代码编写时，某工程师直接将 `input_ids` 深度拷贝赋给了 `labels`（即 `labels = input_ids.clone()`），没有对 Prompt 部分进行任何遮蔽处理。请从交叉熵损失函数计算式与自回归生成的角度，解释这样训练会导致模型产生什么严重的表征异常？

**采分点与解析**：
1. **损失函数计算偏差（4分）**：未对 Prompt 进行遮蔽，导致原本用于过滤无关梯度的 `ignore_index=-100` 失效。模型的 Loss 变为了预测 Prompt 词元和预测 Response 词元的联合损失：$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{prompt}} + \mathcal{L}_{\text{response}}$。
2. **能力与容量损耗（3分）**：模型分配了大量梯度去适应“输入指令的语法风格分布”（如背诵用户问句的开门习惯），分散了对“遵循指令完成复杂推理解答”的参数学习预算。
3. **推理行为异常（3分）**：模型容易出现“指令幻听”或“自问自答”现象，在给出简短回答后，会不受控制地继续模拟生成下一轮虚构的用户提问，破坏正常的终止停机机制。

---

### 题 2 【数据工程架构题】
**题目**：业务团队要微调一个客服助手，历史数据中包含 100,000 条日常查件咨询（多数类），但只有 500 条关于高风险投诉退款的专业应答（少数类）。如果直接采用传统机器学习的“过采样”将这 500 条样本简单复制 200 次混合训练，模型最可能出现什么致命缺陷？在现代 LLM 数据工程中，有哪两种业界标准的科学解决策略？

**采分点与解析**：
1. **致命缺陷剖析（4分）**：严重过拟合与记忆死记（Memorization）。这 500 条特定话术会在参数中被高频强行刻入，导致模型丧失泛化推理能力；在面对投诉类问题时，无论用户具体陈述如何微调，模型都会复读一模一样的死板模板，甚至产生复读死循环。
2. **科学策略一：合成数据演化（Evol-Instruct / Data Expansion）（3分）**：以 500 条高质量样本为种子，利用高级模型通过增加约束、改变背景角色、增加情绪对抗等方式自动扩写生成数千条不重样的多轮真实变体数据。
3. **科学策略二：动态加权与两阶段对齐训练（Dynamic Weighting / Multi-stage SFT）（3分）**：第一阶段使用高质量通识与业务数据完成全局指令遵循；第二阶段使用极小学习率（如 $1\times 10^{-5}$）在小规模高价值投诉数据上做精细退火微调（Annealing），避免简单粗暴的数据混抽。

---

## 4. 极简复习闪卡 (CheatSheet)

| 数据工程维度 | 传统直觉误区 | 工业级最佳实践（黄金法则） |
| :--- | :--- | :--- |
| **数据格式** | 误以为 Alpaca 最通用 | **全面采用 ShareGPT / Messages 格式**，原生支持多轮、系统角色与 Tool Calls。 |
| **格式统一重点** | 纠结中文引号、英文引号或标点 | **对齐 Chat Template 特殊标记** + **严格实施 Loss Masking（Prompt 置为 -100）**。 |
| **数据去重** | 仅做 MD5/SHA256 字符串查重 | **MinHash LSH 模糊去重** + **SemDeDup 高维语义去重**，彻底剔除模式复读。 |
| **长尾少数类平衡** | 简单机械复制过采样 | **严禁粗暴复制**！采用 Evol-Instruct 自动演化合成新样本，或采用精调退火机制。 |
| **质量法则 (LIMA)** | 追求数据量越大越好 | **质量与多样性绝对压倒数量**（1,000 条极品指令的效果胜过 100,000 条低质脏数据）。 |
