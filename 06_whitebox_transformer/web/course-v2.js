(() => {
  const modules = [
    { title: "为什么序列需要新架构", lens: "theory", slides: ["0000", "0084", "0324"], body: ["很多学习任务的输入和输出都带有顺序。例如翻译要把中文词序列变为英文词序列，摘要要把长文变为短文。传统 RNN 能按顺序处理信息，但第 t 步必须等待第 t-1 步完成，难以充分利用 GPU；CNN 可以并行，却需要堆很多层才能把远处信息联系起来。Transformer 的关键选择是让每个位置一次就能和整段序列建立联系。", "这并不意味着 Transformer 没有代价。自注意力需要构造位置两两之间的关系，序列很长时计算和显存都会增长。因此它的优势是更短的信息路径与高并行度，而不是对所有问题都绝对更快。"], concepts: [["序列", "有先后顺序的一组元素，例如一句话中的 token。顺序不同，含义可能不同。"], ["Token", "模型处理的基本单位。它可以是词、子词、字符或图像块，不等同于自然语言中的“一个字”。"]], formula: "RNN: h_t=f(h_{t-1},x_t)；Self-Attention 中任意两个位置的交互路径长度为 O(1)。", misconception: "“任意位置都可见”不等于每个位置都同等重要；权重由后续的注意力计算决定。" },
    { title: "Seq2Seq 与 Transformer 全景", lens: "seq2seq", slides: ["0025", "0047", "1676"], body: ["Seq2Seq 是把一个输入序列转换为另一个输出序列的任务框架。机器翻译中，输入是源语言 token，输出是目标语言 token；两段序列长度可以不同。Transformer 是完成这类任务的一种架构，而不是 Seq2Seq 的同义词。", "经典 Transformer 由 Encoder 和 Decoder 组成。Encoder 把整个输入序列编码为一组上下文表示，通常叫 memory；Decoder 每次根据已生成前缀和 memory 预测下一个 token。网页中的端到端透视实验展示的正是这个闭环。"], concepts: [["Encoder", "负责读取并理解输入序列的模块；输出不是一个单值，而是每个输入位置的上下文表示。"], ["Decoder", "负责生成目标序列的模块。它必须遵守生成顺序，不能读取未来答案。"]], formula: "x_{1:N} → Encoder → memory；y_{<t}, memory → Decoder → p(y_t)。", misconception: "Encoder 不直接“翻译成英文”；它提供可被 Decoder 查询的输入信息。" },
    { title: "Token、Embedding 与位置", lens: "encoder", slides: ["0478", "1075", "1112"], body: ["神经网络不能直接计算离散 token，因此先用 Embedding 查表把每个 token 映射为连续向量。向量维度中的数值不是人工规定的词典释义，而是模型通过训练学到的可计算表示。", "自注意力本身不知道 token 的先后位置：若只交换输入行，关系计算不会自动识别语序变化。因此 Transformer 为每个位置加入位置编码。原始论文使用不同频率的正弦和余弦函数，使位置既有唯一模式，也保留相对距离信息。"], concepts: [["Embedding", "将离散 ID 查表转换为稠密向量的过程；相似用法的 token 往往会形成相关的表示。"], ["位置编码", "与 token 向量同维、逐元素相加的位置向量，用来告诉模型“这是第几个位置”。"]], formula: "X=E+PE，PE(pos,2i)=sin(pos/10000^{2i/d})。", misconception: "相加不会抹去词义；后续线性层可从组合表示中利用词和位置信息。" },
    { title: "Self-Attention：从检索到上下文", lens: "encoder", slides: ["1195", "1338", "1352"], body: ["一个 token 在不同句子中的含义取决于上下文。Self-Attention 把当前位置表示成 Query，把每个可参考位置表示成 Key 和 Value。当前位置用 Query 与所有 Key 比较，得到“应该从哪里取信息”的分数；再以这些分数加权汇总 Value。", "Q、K、V 都来自输入的不同线性投影。这样模型可为“寻找相关对象”“标识可匹配信息”和“提供被汇聚内容”学习不同的特征空间。每个 Query 独立产生一行注意力分布，因此不同 token 可以关注不同位置。"], concepts: [["Query / Key / Value", "Query 表示当前位置要找什么；Key 是用于匹配的索引；Value 是被最终汇聚的内容。三者由不同权重矩阵投影得到。"], ["上下文表示", "融合其他位置信息后得到的新向量。它仍对应原位置，但含有全局语境。"]], formula: "Q=XW_Q，K=XW_K，V=XW_V。", misconception: "注意力权重不是固定语法规则，也不是解释模型的唯一证据；它是当前输入和当前参数下的动态结果。" },
    { title: "缩放点积注意力的完整计算", lens: "encoder", slides: ["1429", "1505", "1527"], body: ["把所有 Query 和 Key 两两点积可得到得分矩阵。矩阵的第 i 行第 j 列表示第 i 个 Query 对第 j 个 Key 的匹配程度。对每一行做 Softmax 后，得分变成非负且和为 1 的权重；再乘 Value 矩阵，就得到每个位置的上下文输出。", "点积维度 d_k 增大时，得分方差也会增大，Softmax 容易接近 0 或 1，梯度会变小。因此在 Softmax 前除以 sqrt(d_k)，把数值尺度拉回更适合学习的范围。请在透视中依次观察 QK^T、缩放、热力图与加权输出。"], concepts: [["Softmax", "把一组任意实数转换为概率式权重：每项大于 0，整行和为 1。它强调相对大小，而非绝对值。"], ["注意力矩阵", "形状通常为 N×N。行是正在查询的位置，列是被参考的位置。"]], formula: "Attention(Q,K,V)=softmax(QK^T/sqrt(d_k))V。", misconception: "除以 sqrt(d_k) 不是归一化概率；概率归一化由 Softmax 完成。" },
    { title: "多头注意力：多种关系同时检索", lens: "encoder", slides: ["1625", "1983", "2028"], body: ["单个注意力空间必须用同一套投影同时处理所有关系。Multi-Head Attention 将模型维度切成多个较小子空间，每个头使用独立的 Q、K、V 投影并行计算，再把各头输出拼接并用 W^O 混合。", "如果模型维度为 D、头数为 h，每头维度通常为 d_k=D/h。注意力主体的计算量约为 h×O(N²D/h)=O(N²D)，因此多头的重点不是增加同类计算，而是提供多个可学习的关系视角。"], concepts: [["Head", "一组独立的注意力投影与计算。不同 head 可能在同一输入上形成不同的关注模式。"], ["Concat", "沿特征维度拼接各头输出，再由输出投影重新混合为模型维度。"]], formula: "MultiHead(Q,K,V)=Concat(head_1,...,head_h)W^O。", misconception: "不能把某个 head 固定解释为“语法头”或“情感头”；这是训练后可能出现的模式，不是结构保证。" },
    { title: "Encoder：稳定地加工上下文", lens: "encoder", slides: ["2123", "2155", "2245"], body: ["一个 Encoder 层包含自注意力、残差连接、LayerNorm 和逐位置前馈网络。自注意力负责在位置之间传递信息；FFN 则对每个位置独立施加相同的非线性变换，让模型进一步加工已经汇聚的上下文。", "残差连接把子层输入与输出相加，使深层网络更容易保持和修正已有信息。LayerNorm 对每个 token 的特征维度做归一化，减少数值尺度波动。多层 Encoder 重复这个结构，形成更抽象的 memory。"], concepts: [["残差连接", "把输入直接加回子层输出：y=x+f(x)。它为信息和梯度保留直接路径。"], ["LayerNorm", "对单个 token 的特征维度计算均值和方差进行归一化，不依赖 batch 大小。"], ["FFN", "对每个位置独立使用的两层非线性网络，通常先升维再降维。"]], formula: "y=LayerNorm(x+Sublayer(x))；FFN(x)=max(0,xW_1+b_1)W_2+b_2。", misconception: "FFN 不负责 token 间通信；跨位置关系由注意力层完成。" },
    { title: "Decoder：遮住未来并读取输入", lens: "decoder", slides: ["2374", "2568", "2606"], body: ["Decoder 的第一个注意力层是 Masked Self-Attention。训练时目标句可以一次输入以提高并行度，但第 t 个位置必须看不到 t 之后的正确答案；因此把未来位置的得分设为极小值，Softmax 后其权重为零。", "随后是 Cross-Attention：Decoder 当前状态提供 Query，Encoder memory 提供 Key 和 Value。它让目标 token 能在生成时动态查询输入序列。最后，FFN 与线性输出层把当前表示变成词表上的 logits，再经 Softmax 得到下一个 token 的概率。"], concepts: [["因果掩码", "禁止当前位置读取未来目标位置的规则。它保证训练目标和推理条件一致。"], ["Cross-Attention", "Query 来自 Decoder，Key/Value 来自 Encoder memory，用于把生成需求与输入信息对齐。"], ["Logits", "Softmax 之前的未归一化分数；可为任意实数。"]], formula: "mask_{ij}=-∞ (j>i)；p(y_t)=softmax(W_o h_t+b_o)。", misconception: "掩码不是删除 token；它只限制某次注意力计算中可被查询的位置。" },
    { title: "训练、推理与生成策略", lens: "seq2seq", slides: ["2690", "2750", "2899"], body: ["训练阶段常用 Teacher Forcing：把真实的前一个目标 token 输入 Decoder，并同时计算所有位置的预测损失。这使训练高效，但推理时模型只能使用自己刚生成的 token，早期错误可能被带到后续步骤，形成 exposure bias。", "最简单的推理策略是贪婪搜索，每步选择概率最大的 token。束搜索同时保留若干候选序列，试图以更高的整体概率换取更好的结果。两者都是解码策略，不改变 Transformer 的参数或注意力计算。"], concepts: [["Teacher Forcing", "训练时使用真实目标前缀作为输入的方式，使所有目标位置可并行计算。"], ["暴露偏差", "训练看到真实历史、推理看到模型自身历史造成的分布差异。"], ["束搜索", "保留多个候选前缀并逐步扩展的搜索方法，束宽越大计算越多。"]], formula: "L=-Σ_t log p(y_t^*|y_{<t}^*,x)。", misconception: "束搜索不是训练方法；它只在已有模型的推理阶段选择序列。" },
    { title: "把全流程连起来，并理解边界", lens: "seq2seq", slides: ["2950", "2899", "2750"], body: ["完整流程从 token 化、Embedding 和位置编码开始。Encoder 通过多层注意力和 FFN 形成 memory；Decoder 用因果掩码处理已生成前缀，用交叉注意力读取 memory，并经 Softmax 选择下一个 token，直到生成结束标记。建议回到端到端透视，逐步核对每个张量的形状。", "Transformer 的思想也延伸到视觉：ViT 将图像分块视作 token。但注意力的二次复杂度、数据规模需求、位置表示选择和生成错误累积仍是实际限制。理解这些边界比只记住公式更重要。"], concepts: [["自回归生成", "把上一步生成结果作为下一步输入，按顺序生成完整序列。"], ["ViT", "Vision Transformer，把图像块嵌入为 token 后使用 Transformer 建模。"]], formula: "x→Encoder→memory；<BOS>→Decoder→y_1→...→<EOS>。", misconception: "“Attention Is All You Need”是论文标题，不意味着任何场景都只需要注意力。" }
  ];

  const escape = text => text.replace(/[&<>]/g, char => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;" }[char]));
  const prose = text => `<p>${escape(text)}</p>`;
  const slides = (ids, index) => `<details class="source-reference"><summary>查看本模块的原始课程材料</summary><p>以下截图和原始讲稿用于追溯课程来源；它们不是本页的主讲正文。</p><div class="source-slides">${ids.map(id => `<img src="../images/slide_${id}s.jpg" alt="模块 ${index + 1} 的原始课程截图">`).join("")}</div><a href="../Transformer_Full_Instructional_Course.md" target="_blank" rel="noreferrer">打开完整原始讲稿</a></details>`;

  function render(index) {
    const item = modules[index];
    document.body.classList.add("course-mode");
    document.getElementById("sidebar-category-title").textContent = "课程模块";
    document.getElementById("step-nav-list").innerHTML = modules.map((module, i) => `<li><button class="step-item ${i === index ? "active" : ""}" data-module="${i}" ${i === index ? 'aria-current="page"' : ""}><span class="step-num">${String(i + 1).padStart(2, "0")}</span><span>${module.title}</span></button></li>`).join("");
    document.querySelectorAll("[data-module]").forEach(button => button.addEventListener("click", () => render(Number(button.dataset.module))));
    document.getElementById("main-content-view").innerHTML = `<article class="course-page"><header class="course-header"><p>Transformer 交互式课程 · 模块 ${index + 1}/10</p><h1>${item.title}</h1><p>先读清概念，再打开透视实验检查计算。</p></header><section class="lesson-prose"><h2>核心讲解</h2>${item.body.map(prose).join("")}</section><section class="concept-flow"><h2>讲到这里，需要理解的概念</h2>${item.concepts.map(([name, text]) => `<aside class="concept-card"><h3>${name}</h3><p>${text}</p></aside>`).join("")}</section><section class="math-panel"><h2>公式与结构</h2><code>${escape(item.formula)}</code></section><section class="misconception"><h2>避免误解</h2><p>${item.misconception}</p></section><section class="lens-panel"><div><h2>进入透视实验</h2><p>教学玩具模型：数值为固定演示值，不是训练权重。透视中可以逐步检查公式、矩阵与注意力热图。</p></div><button class="btn-nav primary" data-lens="${item.lens}">打开透视</button></section>${slides(item.slides, index)}</article>`;
    document.querySelector("[data-lens]").addEventListener("click", event => {
      const tab = document.querySelector(`[data-tab="${event.currentTarget.dataset.lens}"]`);
      if (tab) tab.click();
    });
  }

  window.addEventListener("DOMContentLoaded", () => {
    document.getElementById("course-tab").addEventListener("click", () => render(0));
    document.querySelectorAll(".nav-tab[data-tab]").forEach(tab => tab.addEventListener("click", () => document.body.classList.remove("course-mode")));
  });
})();
